# 接口自动化测试环境配置
# 切换环境：设置环境变量 TEST_ENV=prod（默认 dev）
# 环境间差异主要在这里（base_url、账号等全局变量）
dev:
  base_url: https://jsonplaceholder.typicode.com
  variables: {}

prod:
  base_url: https://jsonplaceholder.typicode.com
  variables: {}
