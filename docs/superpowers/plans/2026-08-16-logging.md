# 日志功能 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为接口自动化测试框架接入标准 `logging`，实现"文件 + 控制台"双输出纯文本日志，支持用例失败时快速定位请求/响应/断言/异常。

**架构：** 新增 `core/logger.py` 统一管理日志（独立 `autotest` logger，`propagate=False`，FileHandler + StreamHandler）；在 `http_client`（请求/响应）、`runner`（用例生命周期/断言失败/异常）埋点；`conftest.pytest_configure` 中初始化并在主进程清空日志文件。

**技术栈：** Python 标准库 `logging`（零新依赖）。

**规格文档：** `docs/superpowers/specs/2026-08-16-logging-design.md`

---

## 文件结构

| 文件 | 类型 | 职责 |
|------|------|------|
| `core/logger.py` | 创建 | `setup_logging` / `get_logger` / `clear_log_file` / `_reset` |
| `tests/test_logger.py` | 创建 | `core.logger` 单元测试 |
| `core/http_client.py` | 修改 | 埋点：请求/响应日志 + 耗时 |
| `core/runner.py` | 修改 | 埋点：用例开始/结束、extract、断言失败、异常 |
| `conftest.py` | 修改 | `pytest_configure` 初始化日志 + 主进程清空 |
| `.gitignore` | 修改 | 忽略 `logs/` |
| `README.md` | 修改 | 新增"运行日志"小节 |

---

## 任务 1：日志模块 `core/logger.py`

**文件：**
- 创建：`core/logger.py`
- 测试：`tests/test_logger.py`

- [ ] **步骤 1：编写失败的测试**

创建 `tests/test_logger.py`：

```python
# -*- coding: utf-8 -*-
"""core.logger 单元测试（单独运行：pytest tests/test_logger.py -v）。"""
from core import logger


def _setup(tmp_path, monkeypatch):
    """重置状态并以 tmp 目录作为日志目录初始化。"""
    logger._reset()
    monkeypatch.setenv("LOG_DIR", str(tmp_path))
    monkeypatch.setenv("LOG_LEVEL", "INFO")
    logger.setup_logging()
    return logger.get_logger()


def _read(tmp_path):
    for h in logger.get_logger().handlers:
        h.flush()
    return (tmp_path / "test.log").read_text(encoding="utf-8")


def test_setup_creates_log_file(tmp_path, monkeypatch):
    _setup(tmp_path, monkeypatch)
    logger.get_logger().info("hello 日志")
    assert "hello 日志" in _read(tmp_path)


def test_level_respected(tmp_path, monkeypatch):
    _setup(tmp_path, monkeypatch)
    logger.get_logger().debug("不该出现")
    assert "不该出现" not in _read(tmp_path)


def test_setup_is_idempotent(tmp_path, monkeypatch):
    _setup(tmp_path, monkeypatch)
    log = logger.get_logger()
    before = len(log.handlers)
    logger.setup_logging()
    assert len(log.handlers) == before
```

- [ ] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_logger.py -v`
预期：FAIL，报错 `AttributeError: module 'core.logger' has no attribute 'setup_logging'`（`core/logger.py` 尚不存在）。

- [ ] **步骤 3：编写实现代码**

创建 `core/logger.py`：

```python
# -*- coding: utf-8 -*-
"""日志初始化：文件 + 控制台双输出，级别/目录用环境变量覆盖。

- 使用独立 logger "autotest"（propagate=False），避免污染 root / 第三方库日志。
- 初始化幂等；目录不可写时降级为仅控制台，不阻断测试。
- 日志文件 `{LOG_DIR}/test.log`，每次运行由主进程清空一次（见 conftest.py）。
"""
import logging
import os

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

LOGGER_NAME = "autotest"
_FORMAT = "%(asctime)s [%(levelname)s] %(message)s"
_initialized = False


def get_logger():
    return logging.getLogger(LOGGER_NAME)


def setup_logging(level=None, log_dir=None):
    """幂等配置 "autotest" logger：StreamHandler(stderr) + FileHandler(logs/test.log)。"""
    global _initialized
    if _initialized:
        return

    logger = logging.getLogger(LOGGER_NAME)
    logger.setLevel(_resolve_level(level))
    logger.propagate = False

    formatter = logging.Formatter(_FORMAT)

    stream = logging.StreamHandler()
    stream.setFormatter(formatter)
    logger.addHandler(stream)

    log_dir = log_dir or os.environ.get("LOG_DIR", os.path.join(BASE_DIR, "logs"))
    try:
        os.makedirs(log_dir, exist_ok=True)
        file_handler = logging.FileHandler(
            os.path.join(log_dir, "test.log"), encoding="utf-8", mode="a"
        )
        file_handler.setFormatter(formatter)
        logger.addHandler(file_handler)
    except OSError:
        logging.getLogger(__name__).warning("日志目录不可写，降级为仅控制台输出")

    _initialized = True


def clear_log_file(log_dir=None):
    """清空日志文件（仅主进程调用一次，保证每次运行覆盖上次日志）。"""
    log_dir = log_dir or os.environ.get("LOG_DIR", os.path.join(BASE_DIR, "logs"))
    try:
        os.makedirs(log_dir, exist_ok=True)
        with open(os.path.join(log_dir, "test.log"), "w", encoding="utf-8"):
            pass
    except OSError:
        pass


def _resolve_level(level):
    if level is None:
        level = os.environ.get("LOG_LEVEL", "INFO")
    return getattr(logging, str(level).upper(), logging.INFO)


def _reset():
    """仅测试用：重置初始化状态并移除 "autotest" logger 上的 handler。"""
    global _initialized
    _initialized = False
    logger = logging.getLogger(LOGGER_NAME)
    for h in list(logger.handlers):
        logger.removeHandler(h)
        h.close()
```

- [ ] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_logger.py -v`
预期：PASS（3 passed）。

- [ ] **步骤 5：Commit**

```bash
git add core/logger.py tests/test_logger.py
git commit -m "feat: 添加日志模块 core.logger（文件+控制台双输出）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 任务 2：HTTP 客户端埋点

**文件：**
- 修改：`core/http_client.py`（整文件重写为下方内容）

- [ ] **步骤 1：实现请求/响应日志**

将 `core/http_client.py` 替换为：

```python
# -*- coding: utf-8 -*-
"""HTTP 请求封装：统一超时、Session 复用、请求/响应日志。"""
import time

import requests

from .logger import get_logger

logger = get_logger()


class HttpClient:
    def __init__(self, timeout=30):
        self.session = requests.Session()
        self.timeout = timeout

    def request(self, method, url, **kwargs):
        kwargs.setdefault("timeout", self.timeout)
        start = time.perf_counter()
        resp = self.session.request(method.upper(), url, **kwargs)
        elapsed_ms = (time.perf_counter() - start) * 1000

        logger.info("请求 %s %s", method.upper(), url)
        logger.debug("请求头 %s", kwargs.get("headers"))
        logger.debug("请求体 %s", _body_of(kwargs))
        logger.info("响应 %s %s（耗时 %.0fms）", resp.status_code, url, elapsed_ms)
        logger.debug("响应体 %s", resp.text)
        return resp


def _body_of(kwargs):
    """取请求体（json 优先，其次 data/params），便于 DEBUG 排查。"""
    for key in ("json", "data", "params"):
        if kwargs.get(key) is not None:
            return kwargs[key]
    return None
```

- [ ] **步骤 2：验证无语法/导入错误**

运行：`python -c "from core.http_client import HttpClient"`（在项目根目录）
预期：无报错退出。

- [ ] **步骤 3：Commit**

```bash
git add core/http_client.py
git commit -m "feat: HTTP 客户端记录请求/响应日志与耗时

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 任务 3：执行器埋点

**文件：**
- 修改：`core/runner.py`（整文件重写为下方内容）

- [ ] **步骤 1：实现用例生命周期日志**

将 `core/runner.py` 替换为：

```python
# -*- coding: utf-8 -*-
"""用例执行器：变量替换 -> 发请求 -> 提取 -> 断言。"""
from .context import get_context, set_var
from .http_client import HttpClient
from .logger import get_logger
from .validators import get_value, run_validators
from .variables import substitute

logger = get_logger()


def build_ctx(case_file, file_data, env, row_vars):
    """构建变量上下文：环境变量 < 文件级变量 < Excel 行数据 < 会话共享变量(extract)。"""
    ctx = {"BASE_URL": env.get("base_url", "")}
    ctx.update(env.get("variables", {}))
    ctx.update(file_data.get("variables", {}))
    ctx.update(row_vars or {})
    ctx.update(get_context(case_file))

    # 模块级 base_url 覆盖（支持 $ 变量引用，如不同微服务网关）
    if file_data.get("base_url"):
        ctx["BASE_URL"] = substitute(file_data["base_url"], ctx)
    return ctx


def run_case(case_file, file_data, case, row_vars, env):
    module = file_data.get("name", "未命名模块")
    name = case.get("name", "未命名用例")
    logger.info("用例开始：%s::%s", module, name)
    try:
        ctx = build_ctx(case_file, file_data, env, row_vars)
        case = substitute(case, ctx)

        req = case["request"]
        method = req["method"]
        url = req["url"]
        if not url.startswith(("http://", "https://")):
            url = ctx["BASE_URL"].rstrip("/") + "/" + url.lstrip("/")

        kwargs = {k: v for k, v in req.items() if k not in ("method", "url")}
        resp = HttpClient().request(method, url, **kwargs)

        # 断言前写入报告，失败时也能保留请求/响应信息便于排查
        _attach(method, url, resp)

        # 提取响应字段到会话共享上下文，供后续用例引用（如登录后传 token）
        for key, expr in case.get("extract", {}).items():
            value = get_value(resp, expr)
            set_var(case_file, key, value)
            logger.debug("提取 %s = %r", key, value)

        run_validators(resp, case.get("validate", []))
    except AssertionError as e:
        logger.error("断言失败：%s::%s — %s", module, name, e)
        raise
    except Exception:
        logger.exception("用例异常：%s::%s", module, name)
        raise
    else:
        logger.info("用例结束：PASS %s::%s", module, name)
        return resp


def _attach(method, url, resp):
    try:
        import allure
    except ImportError:
        return
    allure.attach(f"{method.upper()} {url}", "请求", allure.attachment_type.TEXT)
    allure.attach(f"{resp.status_code}\n{resp.text}", "响应", allure.attachment_type.TEXT)
```

- [ ] **步骤 2：验证无语法/导入错误**

运行：`python -c "from core.runner import run_case"`（在项目根目录）
预期：无报错退出。

- [ ] **步骤 3：Commit**

```bash
git add core/runner.py
git commit -m "feat: 执行器记录用例生命周期/断言失败/异常日志

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 任务 4：pytest 入口初始化日志

**文件：**
- 修改：`conftest.py`

- [ ] **步骤 1：在 `pytest_configure` 中初始化并清空日志**

在 `conftest.py` 顶部 import 处加入 `from core.logger import clear_log_file, setup_logging`，并新增 `pytest_configure` 钩子：

```python
from core.logger import clear_log_file, setup_logging  # noqa: E402
from core.notifier import notify  # noqa: E402
from core.yaml_loader import load_cases  # noqa: E402

# 记录每个用例的执行结果，供 session 结束时的结果通知使用
_results = {}


def pytest_configure(config):
    setup_logging()
    # 每次运行覆盖上次日志：仅主进程清空一次；xdist worker 不清空，避免互相覆盖
    if os.environ.get("PYTEST_XDIST_WORKER") is None:
        clear_log_file()
```

（其余函数 `pytest_generate_tests` / `pytest_runtest_makereport` / `pytest_sessionfinish` 保持不变。）

- [ ] **步骤 2：验证收集正常**

运行：`pytest --collect-only -q`
预期：正常列出 `test_api.py` 下的用例，无异常退出。

- [ ] **步骤 3：Commit**

```bash
git add conftest.py
git commit -m "feat: pytest 入口初始化日志并在主进程清空日志文件

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 任务 5：忽略日志目录 + 文档

**文件：**
- 修改：`.gitignore`
- 修改：`README.md`

- [ ] **步骤 1：`.gitignore` 忽略 `logs/`**

在 `.gitignore` 的 `# Allure 报告（生成物）` 段落后追加：

```gitignore
# 运行日志
logs/
```

- [ ] **步骤 2：README 新增"运行日志"小节**

在 `## 测试报告（Allure）` 小节之前插入：

```markdown
## 运行日志

每次运行会在 `logs/test.log` 落一份纯文本日志（同时打印到控制台），用于用例失败排查。

- **位置**：`logs/test.log`（每次运行覆盖上一次）
- **级别**：默认 `INFO`；设环境变量 `LOG_LEVEL=DEBUG` 打印完整请求/响应头与 body
- **排查**：失败时按用例名 grep，例如 `grep "用例名" logs/test.log`，可看到请求/响应/断言失败详情/异常堆栈

```bash
LOG_LEVEL=DEBUG pytest     # 更详细（含请求/响应头与 body）
```
```

- [ ] **步骤 3：Commit**

```bash
git add .gitignore README.md
git commit -m "docs: 忽略 logs 目录并补充运行日志说明

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 任务 6：端到端验证

**文件：** 无新增/修改（仅运行验证，含一次临时失败用例）

- [ ] **步骤 1：跑 demo 用例，确认日志文件生成**

运行：`pytest`
预期：全部通过；`logs/test.log` 生成，包含 `用例开始`、`请求 GET https://...`、`响应 200`、`用例结束：PASS` 等行。

- [ ] **步骤 2：故意制造失败，确认错误日志**

临时创建 `cases/demo/_tmp_fail.yaml`：

```yaml
name: 临时失败用例
cases:
  - name: 必失败
    request:
      method: GET
      url: /todos/1
    validate:
      - eq: [status_code, 999]
```

运行：`pytest -k "必失败"`（预期 1 failed，其余 deselected）
检查：`grep "断言失败" logs/test.log` 应命中，且附近有 `响应 200`。
然后删除 `cases/demo/_tmp_fail.yaml`（不提交）。

- [ ] **步骤 3：DEBUG 级别确认请求/响应体原样打印**

运行：`LOG_LEVEL=DEBUG pytest -k "demo"`
检查：`grep "响应体" logs/test.log` 应命中，内容为接口返回的 JSON。

- [ ] **步骤 4：并发下日志正常**

运行：`pytest -n 4`
检查：`logs/test.log` 正常写入，包含全部用例（允许行交错，每条含时间戳）。

- [ ] **步骤 5：确认无遗漏**

运行：`git status`
预期：工作区仅有 `logs/`（已被忽略，不显示）与可能的临时文件已清理；无 `cases/demo/_tmp_fail.yaml` 残留。

---

## 自检结论

- **规格覆盖度**：规格 §4（logger / http_client / runner / conftest）、§6（配置）、§8（错误处理降级）、§9（验证）、§10（文档）、§11（范围）均有对应任务。规格 §4.4 xdist 清空策略落在任务 4 `pytest_configure`。
- **类型一致性**：`get_logger()` 无参返回 `autotest` logger；三处埋点均 `from .logger import get_logger`；`clear_log_file` / `setup_logging` 签名在任务 1 定义、任务 4 调用一致。
- **占位符**：无 TODO/待定；所有步骤含完整代码与命令。
